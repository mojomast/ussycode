---
description: Check and report the public URL for this VM's web service
---
Check if there's a web service running on port 8080 (or the most likely dev server port). Use the `ussycode_publish` tool to verify and report the public URL. If nothing is running, suggest how to start a simple server.
